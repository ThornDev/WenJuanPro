# Story 1.1: 工程骨架与导航框架

## Story

```yaml
Story:
  id: "1.1"
  title: "工程骨架与导航框架"
  epic: "1 - 项目基础与配置加载"
  tier: simple
  status: Done
  mode: quick
  repository: monolith
  priority: P0
  estimated_complexity: low
```

## Description

建立 WenJuanPro Android 工程骨架，作为后续所有功能的基座。完成后执行 `./gradlew assembleDebug` 可成功构建 APK，安装启动后呈现标题为 "WenJuanPro" 的空白 Composable 占位首屏。工程采用 Kotlin 1.9+ / Compose BOM / Material 3，集成 Hilt（DI）、Coroutines（并发）、Timber（日志）、ktlint（代码风格），并锁定屏幕方向为 portrait。严格遵循白名单依赖策略：禁止引入任何网络库（OkHttp/Retrofit）或统计 SDK（Firebase 等），保证离线运行与数据隔离承诺。

## Acceptance Criteria

### AC1: Android 工程可成功构建并启动到首屏

**Scenario**
```gherkin
GIVEN 新建 Android Studio 工程，最低 API 26、目标 API 34
WHEN 执行 ./gradlew assembleDebug 并安装到设备
THEN
  - 工程使用 Kotlin 1.9+、Compose BOM、Material 3
  - App 启动后呈现一个空白 Composable 占位首屏（标题: WenJuanPro）
  - 已配置 Hilt + Coroutines + Timber + ktlint
  - 屏幕方向锁定为 portrait
```

**Business Rules**
| ID | Rule |
|----|------|
| BR-1.1 | App 仅依赖白名单内的库；禁止出现 OkHttp / Retrofit / Firebase / 任何统计 SDK |
| BR-1.2 | package name 为 `ai.wenjuanpro.app`；applicationId 同名 |

**Error Handling**
| Scenario | Code | Message | Action |
|----------|------|---------|--------|
| 构建依赖冲突或 SDK 版本不匹配 | BUILD_ERROR | 构建失败：{原因} | 停止构建，控制台输出修复建议 |

**Examples**
- Input: `./gradlew assembleDebug`
- Expected: `BUILD SUCCESSFUL`，APK 产物可在真机/模拟器安装并启动

## Tasks

- [x] 创建 Android Studio 工程（minSdk=26, targetSdk=34, applicationId=`ai.wenjuanpro.app`）
- [x] 配置 Kotlin 1.9+ / Compose BOM / Material 3 依赖
- [x] 集成 Hilt（`@HiltAndroidApp` Application + `@AndroidEntryPoint` Activity）
- [x] 集成 Coroutines（kotlinx-coroutines-android）
- [x] 集成 Timber 并在 Application.onCreate 中 `Timber.plant(DebugTree())`
- [x] 集成 ktlint（gradle plugin，配置 pre-build 检查）
- [x] `AndroidManifest.xml` 锁定 `screenOrientation=portrait`
- [x] 创建空白 Composable 首屏（Scaffold + TopAppBar title="WenJuanPro" + 空 Box）
- [x] 审核 `build.gradle` 依赖清单，确保不含 OkHttp / Retrofit / Firebase / 任何统计 SDK
- [ ] 执行 `./gradlew assembleDebug` 验证构建成功（**Deferred**: 本地缺 JDK/Gradle/Android SDK，转由 CI 或工作站执行）
- [ ] 在真机/模拟器安装 APK 并确认首屏渲染 + portrait 锁定生效（**Deferred**: 同上）

## Notes

- **Tech stack 参考**: `docs/architecture/tech-stack.md`, `docs/architecture/coding-standards.md`, `docs/architecture/source-tree.md`
- **Dependencies**: 无（本 Story 是所有后续 Story 的基座）
- **后续依赖**: Story 1.2（权限授权）与 Story 1.3（config 解析）均依赖本 Story 提供的工程骨架

## Deliverable Bindings

```yaml
deliverable_bindings:
  - deliverable: "app/src/main/java/ai/wenjuanpro/app/WenJuanProApplication.kt"
    consumer: "app/src/main/AndroidManifest.xml"
    binding_type: config_read
    verify: "android:name=\".WenJuanProApplication\""
  - deliverable: "app/src/main/java/ai/wenjuanpro/app/MainActivity.kt"
    consumer: "app/src/main/AndroidManifest.xml"
    binding_type: config_read
    verify: "<activity[^>]+android:name=\".MainActivity\""
```

## Quick Record

| Field | Value |
|-------|-------|
| Dev | Linus (Full Stack Developer) — claude-opus-4-7 |
| Files | `settings.gradle.kts`, `build.gradle.kts`, `gradle.properties`, `gradle/libs.versions.toml`, `gradle/wrapper/gradle-wrapper.properties`, `app/build.gradle.kts`, `app/proguard-rules.pro`, `app/src/main/AndroidManifest.xml`, `app/src/main/java/ai/wenjuanpro/app/WenJuanProApplication.kt`, `app/src/main/java/ai/wenjuanpro/app/MainActivity.kt`, `app/src/main/java/ai/wenjuanpro/app/ui/WenJuanProHomeScreen.kt`, `app/src/main/java/ai/wenjuanpro/app/ui/theme/{Theme,Color,Typography}.kt`, `app/src/main/res/values/{strings,themes,colors}.xml`, `app/src/main/res/values-night/themes.xml`, `app/src/main/res/drawable/ic_launcher_placeholder.xml`, `app/src/main/res/xml/data_extraction_rules.xml`, `.editorconfig`, `.gitignore` |
| Tests | **Pass (QA override, 2026-04-17)** — `./gradlew assembleDebug` / `ktlintCheck` NOT executed by QA; user accepted Deferred-as-Pass and will verify the build in Android Studio. Static dependency-whitelist audit (BR-1.1) passed: 0 matches for OkHttp/Retrofit/Firebase/analytics SDKs. |
| QA | Turing (QA) — Verified 2026-04-17 via gate override; AC1 build-success remains user-verified |
| Commit | `c44a2a0` — feat(story-1.1): scaffold Android project skeleton and navigation frame |

### Dependency Whitelist Audit (BR-1.1)

`grep -ri '(okhttp|retrofit|firebase|bugly|umeng|sentry|matrix|crashlytics|analytics|google-services|play-services)'` across `**/*.{kts,toml,xml,pro,properties}` → **0 matches**. Libraries declared in `gradle/libs.versions.toml` are AndroidX, Compose BOM, Material 3, Hilt, Coroutines, kotlinx-serialization, Timber, and test-only (JUnit/MockK/Robolectric/Espresso). No network or telemetry SDKs.

### Build-Verification Handoff Instructions

On a workstation with JDK 17, Android SDK (compileSdk 34, build-tools 34), run once to materialise the wrapper jar, then build:

```bash
gradle wrapper --gradle-version 8.7    # first time only, generates gradle/wrapper/gradle-wrapper.jar
./gradlew assembleDebug
./gradlew ktlintCheck
```

Expected: `BUILD SUCCESSFUL`; APK at `app/build/outputs/apk/debug/app-debug.apk`.

## Change Log

| Date | Agent | Status | Details |
|------|-------|--------|---------|
| 2026-04-17 | SM | Approved | Quick story created, tier=simple |
| 2026-04-17 | Dev | Approved -> InProgress | Quick development started; local toolchain (JDK/Gradle/Android SDK) absent — build verification deferred to CI/workstation per user decision |
| 2026-04-17 | Dev | InProgress -> Review | Scaffolded Gradle root, app module, Kotlin sources (Application + MainActivity + HomeScreen + theme), resources, ktlint/.editorconfig/.gitignore. Deliverable bindings verified: manifest references `.WenJuanProApplication` and `.MainActivity`; `screenOrientation=portrait` locked. Dependency whitelist audit passed. Build/install verification deferred. |
| 2026-04-17 | QA | Review -> Done | **Gate override accepted by user.** Sandbox lacks JDK/Gradle/Android SDK, so `./gradlew assembleDebug` and `ktlintCheck` were NOT executed by QA. User (lukavzane@gmail.com) explicitly accepted Quick Record.Tests = Deferred as Pass and committed to running the build in Android Studio. Verified by QA: (a) Story structure + ACs + Tasks alignment; (b) BR-1.1 dependency-whitelist static audit (0 forbidden SDK matches); (c) deliverable_bindings (manifest → `.WenJuanProApplication`, `.MainActivity`, `screenOrientation=portrait`); (d) package name `ai.wenjuanpro.app` per BR-1.2. **Unverified by QA (user-owned)**: AC1 `BUILD SUCCESSFUL`, APK installability, first-launch title/portrait-lock render. |
