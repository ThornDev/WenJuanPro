# Test Design: 1.4 测评集选择页（首屏）

2026-04-17 | Turing (QA)

## Overview

| Metric | Value |
|--------|-------|
| Total scenarios | 53 |
| Unit | 25 (47%) |
| Integration | 6 (11%) |
| E2E (Compose UI / androidTest) | 22 (42%) |
| P0 | 21 |
| P1 | 24 |
| P2 | 8 |
| Blind-spot scenarios | 14 |
| test_design_level | Standard |

**Story type**: Full-stack / Frontend UI（Android Compose + MVI + Hilt + 本地 I/O）
**Applicable blind-spot categories**（per `blind-spot-categories.yaml` applicability.frontend_ui）:
- **High priority**: BOUNDARY, FLOW, ERROR
- **Medium priority**: CONCURRENCY, DATA

**Testing battlegrounds**:
1. **JVM 单测** (`app/src/test/`)：`StandardTestDispatcher` + `runTest` + MockK — 主战场，覆盖 VM / Mapper / SessionStateHolder / UseCase。
2. **JVM 集成测试** (`app/src/test/`，JUnit4 + `TemporaryFolder`)：`OkioFileSystem.mkdirs()`、`ConfigRepositoryImpl.ensureConfigDir()`。
3. **Compose UI 测试** (`app/src/androidTest/`，`@HiltAndroidTest` + `createAndroidComposeRule<MainActivity>`)：组件级（Card / Sheet / HiddenLongPress）+ Screen 级（含导航）。

---

## Scenarios by AC

### AC1: 展示所有 config 文件并区分有效/损坏

**覆盖目标**：BR-1.1 ~ BR-1.11（11 条业务规则）、3 条 ErrorCode（CONFIG_DIR_MISSING / SCAN_TIMEOUT / CONFIG_SCAN_FAILED）、6 条 UI Interaction、6 条 Examples、Epic 1 首屏可见功能闭环。

#### Core Unit — ConfigListViewModel（MVI + 5s 超时 + Effect 通道）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-UNIT-001 | U | P0 | `init { onIntent(Intent.Refresh) }` 触发后，`advanceUntilIdle()` 前 UiState=`Loading`；`refresh()` 在 `viewModelScope.launch(ioDispatcher)` 上执行 | BR-1.4 进页自动加载 + Dispatcher 切换 |
| 1.4-UNIT-002 | U | P0 | 假 repo 返回 `[Valid(cog-mem), Valid(emo-reg), Invalid(broken)]` → `advanceUntilIdle()` → UiState=`Success(cards.size=3, allInvalid=false)`；有效卡 `isValid=true, questionCount=N`，损坏卡 `questionCount=null, errors.size≥1` | BR-1.1 列表呈现 + BR-1.5 题数徽标 + BR-1.6 状态徽标数据面 |
| 1.4-UNIT-003 | U | P0 | 假 repo 返回 `emptyList()` → UiState=`Empty` | 空目录（例）|
| 1.4-UNIT-004 | U | P0 | 3 份全为 `Invalid` → UiState=`Success(allInvalid=true)`（**不**进 Empty 态） | BR-1.10 全部损坏边界 |
| 1.4-UNIT-005 | U | P0 | 假 repo `loadAll()` 延迟 6s，`advanceTimeBy(5_001)` → `withTimeoutOrNull` 返回 null → UiState=`Timeout` | SCAN_TIMEOUT 错误码主路径 |
| 1.4-UNIT-006 | U | P0 | 假 repo 抛 `RuntimeException("disk full")` → UiState=`Error(messageRes=config_list_error_title)`；Timber.e 只记录 `javaClass.simpleName + errorCode`（不含用户明文） | CONFIG_SCAN_FAILED 错误码 + 日志内容过滤（coding-standards） |
| 1.4-UNIT-007 | U | P0 | `CardClicked("cog-mem")` → `SessionStateHolder.selectedConfigId.value == "cog-mem"` + Effect 通道产出单个 `NavigateToScan("cog-mem")` | BR-1.2 选择 config 并派发导航 |
| 1.4-UNIT-008 | U | P1 | `HiddenAreaLongPressed` → Effect 通道产出 `NavigateToDiagnostics`；UiState **不**变（不影响当前渲染）；Timber.d 记录一次 | BR-1.11 隐藏入口 |
| 1.4-UNIT-009 | U | P1 | `ViewErrors("broken.txt")` → `sheetState.value = ErrorSheetState(sourceFileName="broken.txt", errors=List)`；errors 来自 Invalid.errors | BR-1.7 + BR-1.8 错误 Sheet 触发 |
| 1.4-UNIT-010 | U | P1 | `DismissSheet` → `sheetState.value == null` | UI Interaction Sheet 关闭 |
| 1.4-UNIT-011 | U | P0 | 在 `refresh()` 中，`configRepository.ensureConfigDir()` **必须在** `loadConfigs()` 之前被调用（顺序断言：通过 MockK `verifyOrder`） | CONFIG_DIR_MISSING 自动兜底 |
| 1.4-UNIT-012 | U | P1 | 当前正处于 `Loading`，连续发送 `Intent.Refresh` ×2：后一次无效（不会发起第二次 `loadAll`；或等效地，UI 层通过 `enabled = state !is Loading` 已禁用） | BR-1.4 刷新按钮加载期禁用语义 |

#### Core Unit — ConfigListUiMapper（ConfigLoadResult → ConfigCardUiModel）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-UNIT-013 | U | P0 | `ConfigLoadResult.Valid(Config(configId, title, questions[5]))` → `ConfigCardUiModel(configId, title, questionCount=5, isValid=true, errors=emptyList(), sourceFileName)` | 有效映射契约 |
| 1.4-UNIT-014 | U | P0 | `ConfigLoadResult.Invalid("broken.txt", errors=[ParseError(...)])` → `ConfigCardUiModel(configId="broken", title="broken.txt", questionCount=null, isValid=false, errors=<non-empty>, sourceFileName="broken.txt")` | 损坏映射契约（configId = fileName 去 `.txt`） |
| 1.4-UNIT-015 | U | P1 | `sourceFileName` 在两个分支均按原值透传（LazyColumn key 稳定性依赖） | LazyColumn key 稳定性 |

#### Core Unit — SessionStateHolder（@ActivityRetainedScoped）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-UNIT-016 | U | P0 | `selectConfig("cog-mem")` → `selectedConfigId.value == "cog-mem"` | 状态写入契约（Story 2.1 消费） |
| 1.4-UNIT-017 | U | P1 | 已有 selectedConfigId 后 `clear()` → 值回到 null | 会话清理（未来 Story 4 续答重置） |
| 1.4-UNIT-018 | U | P1 | StateFlow 订阅方在 `selectConfig` 后收到更新（用 `first()` 或 `toList().take(2)` 验证）| 响应式订阅正确性 |

#### Core Unit — LoadConfigsUseCase（薄包装）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-UNIT-019 | U | P1 | `invoke()` 直接透传 `repo.loadAll()` 的结果；无业务增强 | 薄包装契约（未来扩展锚点） |
| 1.4-UNIT-020 | U | P2 | `repo.loadAll()` 返回 `emptyList()` → UseCase 返回 `emptyList()`（非 null、非抛异常） | 空值传递 |

#### Core Integration — OkioFileSystem.mkdirs（新增扩展）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-INT-001 | I | P0 | `TemporaryFolder` 构造不存在的子目录路径 `<tmp>/nested/deep/` → `mkdirs(path)` 返回 true；`java.io.File(path).exists() == true` | 主干：自动创建 config 目录 |
| 1.4-INT-002 | I | P1 | 传入已存在目录 → 返回 true，无异常；目录仍然存在 | 幂等性（BR-1.1 兜底路径） |
| 1.4-INT-003 | I | P2 | 父路径是普通文件（非目录）→ 返回 false；不抛异常；Timber.w 写入一次 | 失败分支（降级为 Empty 态） |

#### Core Integration — ConfigRepositoryImpl.ensureConfigDir（新增）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-INT-004 | I | P0 | 假 FileSystem `exists(CONFIG_DIR)=false` → `ensureConfigDir()` 调 `fileSystem.mkdirs(CONFIG_DIR)` 一次；`withContext(ioDispatcher)` 切线程断言 | 目录创建核心契约 |
| 1.4-INT-005 | I | P1 | 假 FileSystem `exists(CONFIG_DIR)=true` → `ensureConfigDir()` **不**调 `mkdirs`（`verify(exactly = 0)`）| 幂等性（不重复创建） |

#### Core E2E — AssessmentCard（组件测试，androidTest）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-E2E-001 | E | P0 | 有效 `ConfigCardUiModel(isValid=true)` → 整卡可点击；`onClick` 被调用一次 | BR-1.6 有效卡片可点 |
| 1.4-E2E-002 | E | P0 | 损坏 `ConfigCardUiModel(isValid=false)` → 整卡 `Card` 的 `onClick=null`；点击整卡**不**触发 `onClick` lambda；`alpha=0.5f` 可通过 semantics 验证 | BR-1.7 损坏置灰且不可点 |
| 1.4-E2E-003 | E | P0 | 损坏卡片右下角『查看错误详情』`TextButton` 可点击 → `onViewErrors` 被调用一次 | BR-1.7 查看详情入口 |

#### Core E2E — ErrorSheet（组件测试）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-E2E-004 | E | P1 | 传入 3 条 `ParseError`（含 field=null 一条）→ `LazyColumn` 渲染 3 行；每行格式为 `第 {line} 行 · {field\|"—"} · {message}`，等宽字体 | BR-1.8 行号级错误详情 |
| 1.4-E2E-005 | E | P1 | 点击顶部关闭 `IconButton` → `onDismiss` 回调被调用一次 | UI Interaction Sheet 关闭 |

#### Core E2E — HiddenLongPressArea（组件测试）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-E2E-006 | E | P1 | `composeRule.mainClock.autoAdvance = false`；按下后 `advanceTimeBy(5_100)` 再释放 → `onTriggered` 调用一次 | BR-1.11 5s 阈值满足 |
| 1.4-E2E-007 | E | P1 | 按下后 `advanceTimeBy(4_900)` 再释放 → `onTriggered` **不**被调用 | BR-1.11 5s 阈值未满足 |

#### Core E2E — ConfigListScreen（Screen 级集成）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-E2E-008 | E | P0 | 通过 Hilt TestModule 注入返回 2 个 Valid + 1 Invalid 的假 repo → Screen 渲染 3 张卡片；`有效`/`损坏` Chip 文案正确；`5 题` / `? 题` 徽标正确 | AC1 主干可视渲染 |
| 1.4-E2E-009 | E | P0 | 假 repo 返回 `emptyList()` → 空态文案 `请将 config TXT 文件放入 /sdcard/WenJuanPro/config/ 后点击刷新` + 『刷新』按钮可见 | Empty 态 |
| 1.4-E2E-010 | E | P1 | 全部 3 份为 Invalid → 顶部横幅 `当前无可用测评集` 可见 + 3 张损坏卡片渲染 | BR-1.10 全部损坏横幅 |
| 1.4-E2E-011 | E | P0 | 点击某损坏卡『查看错误详情』→ `ModalBottomSheet` 可见 + 至少 1 行错误文本 | BR-1.7 + BR-1.8 端到端 |
| 1.4-E2E-012 | E | P1 | Sheet 可见时点击关闭按钮 → Sheet 消失（`onNodeWithText(...).assertDoesNotExist()`） | Sheet 关闭端到端 |
| 1.4-E2E-013 | E | P1 | 假 repo 延迟 6s → `mainClock.advanceTimeBy(5_001)` → 『扫描题库超时...』+ 『重试』按钮可见；点击重试 → 进入 Loading → Success | SCAN_TIMEOUT 端到端 + 重试 |

#### Core E2E — ConfigListNavigationTest（导航契约）

| ID | Lvl | Pri | Test | Why |
|----|-----|-----|------|-----|
| 1.4-E2E-014 | E | P0 | 预置 1 个有效 config；`createAndroidComposeRule<MainActivity>` + NavHost；点击卡片 → `navController.currentDestination?.route` 以 `scan?configId=` 开头；参数 `configId` 值与点击卡一致 | BR-1.2 NavHost 接入 + Story 2.1 消费契约 |

---

## Blind Spot Scenarios [BLIND-SPOT]

### BOUNDARY（边界值 — 列表长度 / 题数 / 文案长度 / 长按阈值）

| ID | Lvl | Pri | Scenario | Blind-spot Ref |
|----|-----|-----|----------|----------------|
| 1.4-BLIND-BOUNDARY-001 | U | P1 | config 列表长度 N=0 → UiState=`Empty`（与 UNIT-003 共享断言但此条明示边界） | BOUNDARY-001 |
| 1.4-BLIND-BOUNDARY-002 | E | P2 | N=1 → 单卡片正确渲染（LazyColumn 单元素分支） | BOUNDARY-002 |
| 1.4-BLIND-BOUNDARY-003 | E | P2 | N=100（预置 100 个假 ConfigLoadResult）→ LazyColumn 正常渲染首屏；不触发 OOM；`mainClock.advanceTimeBy(200)` 后仍在 Success 态 | BOUNDARY-003 |
| 1.4-BLIND-BOUNDARY-004 | U | P2 | 有效 config `questions.size=0` → Mapper 产出 `questionCount=0`；AssessmentCard 徽标显示 `0 题`（边界但合法） | BOUNDARY-002 |
| 1.4-BLIND-BOUNDARY-005 | E | P2 | `title` 长度 80 字符 → AssessmentCard 以 `maxLines=1` + `ellipsis` 截断（不撑破卡高度） | BOUNDARY-003 |
| 1.4-BLIND-BOUNDARY-006 | E | P1 | HiddenLongPressArea 恰好按住 `thresholdMs = 5000`（边界等于）→ `onTriggered` 调用（`elapsed >= thresholdMs` 的 `>=` 语义锁定） | BOUNDARY-002 |

### ERROR（错误处理 — 权限撤销 / mkdirs 失败 / null field / 重试）

| ID | Lvl | Pri | Scenario | Blind-spot Ref |
|----|-----|-----|----------|----------------|
| 1.4-BLIND-ERROR-001 | U | P1 | 假 repo `loadAll()` 抛 `SecurityException("permission revoked")` → VM 捕获并进入 `Error` 态；**不**崩溃；Timber 只记录 `SecurityException` 类名（不含 message） | ERROR-002 |
| 1.4-BLIND-ERROR-002 | I | P1 | 假 FileSystem `exists=false` + `mkdirs` 返回 false（模拟磁盘只读）→ `ensureConfigDir()` 返回 false；后续 `loadAll` 读到空目录 → UiState=`Empty`（**不**转 Error） | ERROR-005 |
| 1.4-BLIND-ERROR-003 | E | P1 | Invalid config 含 `ParseError(line=2, field=null, code=..., message="...")` → ErrorSheet 该行渲染为 `第 2 行 · — · ...`（`field` 为 null 时 UI 层必须传 `"—"`）| ERROR-003 |
| 1.4-BLIND-ERROR-004 | U | P1 | Timeout 态 → 发送 `Intent.Refresh`（模拟点击重试）→ VM 重新进入 Loading → 再次调用 `ensureConfigDir + loadConfigs`；**不**残留上一轮 `_isLoading` 标志 | ERROR-001 |

### FLOW（流程细节 — 重复点击 / 取消 / 返回键 / 长按滑出）

| ID | Lvl | Pri | Scenario | Blind-spot Ref |
|----|-----|-----|----------|----------------|
| 1.4-BLIND-FLOW-001 | U | P1 | 连续两次 `CardClicked("cog-mem")`（模拟双击 / 误触）→ Effect 通道最多产出 1 条 `NavigateToScan`（通过 `distinctUntilChanged` 或 `consume` 一次性语义保证；或由 UI 层 `throttleFirst` 保证 — Dev 任选一方，在 Dev Log 记录） | FLOW-002 |
| 1.4-BLIND-FLOW-002 | U | P2 | VM `refresh()` 执行中，外部调用 `viewModelScope.cancel()`（模拟页面被回收）→ `loadAll()` 协程被取消；**不**抛 `NullPointerException`；不再写入 UiState | FLOW-001 |
| 1.4-BLIND-FLOW-003 | E | P1 | ErrorSheet 打开状态下按系统返回键 → Sheet 消失（`ModalBottomSheet.onDismissRequest` 触发）；**不**弹栈到上一屏（首屏无前驱） | FLOW-005 |
| 1.4-BLIND-FLOW-004 | E | P2 | HiddenLongPressArea 按下后 3s 手指滑出 Area（`up` 事件超出 Box 边界）→ `tryAwaitRelease` 返回 false → `onTriggered` **不**调用 | FLOW-003 |

---

## Risk Coverage

本故事未单独生成 `risk-profile`（test_design_level=Standard，complexity=1，security_sensitive=false，无 DB/API）。主要风险与对应测试：

| 风险 | 对应测试 |
|------|---------|
| 首屏白屏/卡死（init 未触发 Refresh 或 Dispatcher 错误） | UNIT-001（init→Refresh + ioDispatcher） |
| 主线程 I/O 阻塞 | INT-004（`withContext(ioDispatcher)` 断言）+ UNIT-001 |
| 5s 超时未实现/无法恢复 | UNIT-005 + E2E-013（超时文案+重试闭环） |
| 损坏卡片可被点击（绕过 BR-1.7 → 错误流进入扫码页）| E2E-002（整卡不响应 `onClick`）+ E2E-001/003（onClick vs onViewErrors 分离） |
| 隐藏入口误触（<5s 触发）| E2E-006/007 + BLIND-BOUNDARY-006 + BLIND-FLOW-004 |
| Session 状态丢失（跨 ViewModel 共享失效） | UNIT-016/017/018 + E2E-014（NavHost 参数 + StateFlow 双写） |
| Story 2.1 消费契约破坏（configId 路由参数 / SessionStateHolder API） | UNIT-007 + E2E-014（双通道一致性） |
| 全部损坏被误判为 Empty（边界错乱） | UNIT-004 + E2E-010（allInvalid=true 横幅） |
| 行号级错误文案错位（影响研究员自检） | E2E-004 + BLIND-ERROR-003（field=null → `—` 占位） |
| 日志泄漏题目明文（NFR9）| UNIT-006（Timber 只记 exception 类名 + code） |

---

## Execution Order

1. **P0 Unit**（UNIT-001 ~ 007, 011, 013, 014, 016 — 共 10 条）— 主干 VM / Mapper / Session 契约快速反馈
2. **P0 Integration**（INT-001, INT-004 — 2 条）— `mkdirs` + `ensureConfigDir` 核心路径
3. **P0 E2E**（E2E-001, 002, 003, 008, 009, 011, 014 — 7 条）— 组件点击语义 + Screen 主干 + 导航
4. **P1 Unit / Integration**（UNIT-008 ~ 010, 012, 015, 017 ~ 019；INT-002, 005）
5. **P1 E2E + Blind-spot E2E**（E2E-004 ~ 007, 010, 012, 013；BLIND-BOUNDARY-006；BLIND-ERROR-003；BLIND-FLOW-003）
6. **P1 Blind-spot Unit/Integration**（BLIND-ERROR-001, 002, 004；BLIND-FLOW-001；BLIND-BOUNDARY-001）
7. **P2**（UNIT-020；INT-003；BLIND-BOUNDARY-002 ~ 005；BLIND-FLOW-002, 004）

---

## Coverage Gaps

- **[GAP-1]** BLIND-FLOW-001（重复点击去重策略）：Story Dev Notes 未明确选择"VM 内 `distinctUntilChanged` / Effect channel 一次性 consume / UI 层 `throttleFirst`"中的哪一种。**建议**：由 Dev 在 Dev Log 记录最终实现路径并对应调整本测试断言；默认以"Effect channel 一次性 consume"为契约。
- **[GAP-2]** BLIND-BOUNDARY-003（N=100 的内存/渲染基准）：Story Dev Notes 标注"若 p95 > 4s 则追加分页"但本故事不做分页。本测试仅断言"不崩溃 + 进入 Success 态"；真机性能采样由 Dev Log 承担，不在此测试闭环内。
- **[GAP-3]** E2E-008 ~ 013 的 Hilt TestModule 需替换 `ConfigRepository` 与 `FileSystem`；Story 1.4 SM 未在 Dev Notes 给出 `@TestInstallIn` 的具体模板——**建议** Dev 参考 Story 1.2 `PermissionScreenTest` 的 Hilt 替换模式（已验证可行），或新建 `TestDataModule` 在 `androidTest` 下使用 `@UninstallModules(DataModule::class)`。

---

## Test Skeleton File References

骨架已一并生成（`throw NotImplementedError(...)` 占位，Dev 必须以其为起点实现，所有 TODO 必须被替换为真实断言；不允许删除任何测试方法，如某场景不适用改为 `@Ignore` 并注释原因）：

**JVM 单测 / 集成测试**（`app/src/test/java/ai/wenjuanpro/app/`）：
- `feature/configlist/ConfigListViewModelTest.kt`（UNIT-001 ~ 012 + BLIND-ERROR-001, 004；BLIND-FLOW-001, 002 — 16 个测试方法）
- `feature/configlist/ConfigListUiMapperTest.kt`（UNIT-013 ~ 015 + BLIND-BOUNDARY-004 — 4 个测试方法）
- `domain/session/SessionStateHolderTest.kt`（UNIT-016 ~ 018 — 3 个测试方法）
- `domain/usecase/LoadConfigsUseCaseTest.kt`（UNIT-019 ~ 020 — 2 个测试方法）
- `core/io/OkioFileSystemMkdirsTest.kt`（INT-001 ~ 003 — 3 个测试方法）
- `data/config/ConfigRepositoryImplEnsureConfigDirTest.kt`（INT-004 ~ 005 + BLIND-ERROR-002 — 3 个测试方法）

**Compose UI 测试**（`app/src/androidTest/java/ai/wenjuanpro/app/`）：
- `ui/components/AssessmentCardTest.kt`（E2E-001 ~ 003 + BLIND-BOUNDARY-005 — 4 个测试方法）
- `ui/components/ErrorSheetTest.kt`（E2E-004 ~ 005 + BLIND-ERROR-003 — 3 个测试方法）
- `ui/components/HiddenLongPressAreaTest.kt`（E2E-006 ~ 007 + BLIND-BOUNDARY-006 + BLIND-FLOW-004 — 4 个测试方法）
- `ui/configlist/ConfigListScreenTest.kt`（E2E-008 ~ 013 + BLIND-BOUNDARY-002, 003 + BLIND-FLOW-003 — 9 个测试方法）
- `ui/configlist/ConfigListNavigationTest.kt`（E2E-014 — 1 个测试方法）

**Count verification**: 16+4+3+2+3+3 (JVM = 31) + 4+3+4+9+1 (androidTest = 21) = **52 test methods**（与 53 场景数相差 1 — UNIT-003 和 BLIND-BOUNDARY-001 合并到同一测试方法，注释标注双 ID；此合并显式说明以避免重复断言）。
